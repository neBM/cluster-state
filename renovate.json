{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":automergeMinor",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "timezone": "Europe/London",
  "schedule": [
    "after 10pm every weekday",
    "before 5am every weekday",
    "every weekend"
  ],
  "labels": [
    "dependencies"
  ],
  "packageRules": [
    {
      "description": "Group all Terraform provider updates",
      "matchDatasources": [
        "terraform-provider"
      ],
      "groupName": "terraform-providers",
      "labels": [
        "terraform",
        "dependencies"
      ]
    },
    {
      "description": "Group all Nomad jobspec Docker image updates",
      "matchFileNames": [
        "modules/**/*.hcl"
      ],
      "groupName": "nomad-images",
      "labels": [
        "nomad",
        "docker",
        "dependencies"
      ]
    },
    {
      "description": "Separate major updates from minor/patch",
      "matchUpdateTypes": [
        "major"
      ],
      "labels": [
        "major-update"
      ],
      "automerge": false
    },
    {
      "description": "Auto-merge patch updates",
      "matchUpdateTypes": [
        "patch"
      ],
      "automerge": true
    },
    {
      "description": "Pin Terraform version updates",
      "matchDatasources": [
        "terraform-version"
      ],
      "groupName": "terraform-version"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update Docker images in Nomad HCL jobspecs",
      "datasourceTemplate": "docker",
      "managerFilePatterns": [
        "/\\.hcl$/"
      ],
      "matchStrings": [
        "\\s*image\\s*=\\s*\"(?<depName>.*?):(?<currentValue>.*?)\""
      ]
    },
    {
      "customType": "regex",
      "description": "Update Docker images in Terraform files with renovate comment",
      "datasourceTemplate": "docker",
      "managerFilePatterns": [
        "/\\.tf$/"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*image=(?<depName>[^\\s]+)\\s+\"[^\"]+\"\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ]
    }
  ],
  "ignoreDeps": [],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": [
      "security"
    ]
  }
}
