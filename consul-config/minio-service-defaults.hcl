# MinIO service defaults
# Use HTTP protocol for L7 features in service mesh
Kind                  = "service-defaults"
Name                  = "minio-minio"
Protocol              = "http"
LocalRequestTimeoutMs = 300000
