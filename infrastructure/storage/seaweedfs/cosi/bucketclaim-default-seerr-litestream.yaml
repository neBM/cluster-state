apiVersion: objectstorage.k8s.io/v1alpha1
kind: BucketClaim
metadata:
  labels:
    app: seerr
    environment: prod
    migration-role: active-backup
  name: seerr-litestream
  namespace: default
spec:
  bucketClassName: seaweedfs
  existingBucketName: seerr-litestream
  protocols:
    - s3
