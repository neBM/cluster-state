apiVersion: objectstorage.k8s.io/v1alpha1
kind: BucketClaim
metadata:
  labels:
    app: renovate
    environment: prod
  name: renovate-cache
  namespace: default
spec:
  bucketClassName: seaweedfs
  existingBucketName: renovate-cache
  protocols:
    - s3
