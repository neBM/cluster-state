# Scoped restic backup excludes.
# Keep these generic and safe across restore-critical PVCs; do not add legacy
# whole-root exclusions here.

# Temporary and log files
*.tmp
*.log
*.sock
*.lock

# Dovecot regenerable index/cache files
# Archived corrupt uidlist copies are not active mailbox data and may be
# unreadable after the original metadata incident.
dovecot.index*
dovecot.list.index*
/data/dovecot-mailboxes-sw/**/dovecot-uidlist.corrupt-*

# SQLite temp files
*-wal
*-shm

# Cache directories
cache
.cache

# Log directories
logs
log
