# The strategic CI-storage isolation is now in place. Restore the generic
# runner to one job per cluster node; required pod anti-affinity keeps CI job
# pods distributed while the isolated CI builds/cache/container paths prevent
# their mutable workload from sharing the embedded-etcd storage path.
concurrent = 3
