# Keep generic CI conservative on this three-node embedded-etcd cluster. A
# monitored trial at concurrent=2 reduced serialization but reintroduced etcd
# heartbeat/fdatasync pressure on the embedded control plane, so keep k8s-any
# single-lane until the runner/storage architecture is improved.
concurrent = 1
