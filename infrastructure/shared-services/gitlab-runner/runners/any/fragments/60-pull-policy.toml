    # Avoid re-pulling large mutable toolchain images on every generic job; the
    # repeated image-pull/write churn shares disks with embedded etcd.
    pull_policy = ["if-not-present"]
    allowed_pull_policies = ["always", "if-not-present", "never"]
