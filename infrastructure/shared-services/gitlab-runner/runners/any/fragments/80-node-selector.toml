# Intentionally no hard node selector here: k8s-any keeps generic CI capacity
# across the full runner fleet. Control-plane safety is handled by global runner
# concurrency=1, hard one-CI-job-per-node anti-affinity, and keeping heavier
# service/Testcontainers jobs on amd64.
