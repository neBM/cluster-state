  # k8s-any carries generic MR/default workloads. Keep the hard 1-core
  # limit, but request less than a full core for scheduler flexibility without
  # increasing execution parallelism.
  cpu_request = "500m"
  cpu_limit = "1000m"
