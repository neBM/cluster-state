apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: hermes-webhook-hestia
  namespace: default
  labels:
    kubernetes.io/service-name: hermes-webhook
    app.kubernetes.io/name: hermes-webhook
    app.kubernetes.io/component: automation
addressType: IPv4
ports:
- name: http
  protocol: TCP
  port: 8644
endpoints:
- addresses:
  - 192.168.1.5
  conditions:
    ready: true
